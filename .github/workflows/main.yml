name: First workflow
run-name: My main Workflow

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: running some shell echos
        run: echo "Hi $HOSTNAME"
        
      - name: checkout repository
        uses: actions/checkout@v6
    
      - name: list repository files
        run: ls -la
